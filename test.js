const find_max = (nums) => {
  let max_num = Number.NEGATIVE_INFINITY;
  for (let num of nums) {
    console.log(num);
  }
};
const jorge = 10;

for (let num of jorge) {
  console.log(num);
}
